doctype html
html
  head
    title Users List
  body
    h1 Users List
    if message
      p #{message}
    else
      ul
        each user in users
          li
            | #{user.first_name} #{user.last_name} (ID: #{user.id})
